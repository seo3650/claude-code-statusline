# Merge into your existing [tui]; never replace the whole configuration.
[tui]
status_line = ["current-dir", "git-branch", "context-used-bar", "five-hour-limit-bar", "weekly-limit-bar", "model", "reasoning"]
status_line_use_colors = true
